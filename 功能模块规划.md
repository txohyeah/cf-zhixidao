# 智习岛 · 功能模块规划

> 2026-09-24 定稿（P1.5 上线时）。原则：**条件触发才建页**，不预建空模块；打卡流永远留在总览第一屏。

## 一、信息架构

```
zhixidao.pages.dev（登录后）
├─ 总览 / index.html（v9.1 起 = 双栏 dashboard；<900px 回退单栏，打卡流在前）
│   ├─ 左栏 · 打卡流：14天点阵+连胜 → 13 关卡打卡（全对☆/有错重来）→ 用法说明
│   └─ 右栏 · 信息流（position:sticky，滚动时固定可见）
│       ├─ 🔥 错题弱项（chips + 折叠列表，默认 5 条）
│       └─ 考卷摘要：最近 3 张折叠行（点开看分析）+「全部考卷 · 管理 →」
├─ 考卷库 /papers.html（P1.5）
│   ├─ 时间线（20 张/页，「加载更早」分页）
│   ├─ 新增 / 编辑表单：日期 · 标题 · 科目 · 学期 · 分析(markdown)
│   └─ 删除（confirm 二次确认）；家长可见操作按钮，kid 只读
└─ 纸卡 /cards.html（打印/PDF）
```

## 二、数据层

| 模块 | 表 | 现状 | 容量预估 |
|------|----|------|---------|
| 打卡 | levels / checkin | 13 关闭环 | 每单元加一套关卡，量小 |
| 错题 | wrong_questions / weak_points（副本，只读） | LIMIT 200/30 兜底 | 几百条内无压力 |
| 考卷 | papers | **subject + term 列已就位**（v9 迁移） | 每学期 10-20 张，分页可撑数年 |

papers 字段约定：
- `subject`：chinese / math（english 预留）— API 层白名单校验
- `term`：`2026秋` / `2027春` … — 自由文本，最长 20 字
- `title` 命名建议带单元信息：`语文二上第二单元巩固练`、`数学二上期中卷`

## 三、分期与触发条件

| 期 | 内容 | 状态 | 触发条件 / 说明 |
|----|------|------|----------------|
| P0 | 总览+打卡+考卷时间线+改密码 | ✅ 上线 | — |
| P1 | cuoti 错题弱项副本 | ✅ 上线 | — |
| P1.5 | 考卷库（CRUD+分页+科目/学期） | ✅ 上线 2026-09-24 | 考卷会持续增多，先管起来 |
| P2 | kid 账号 · 生字本 · 周报推送 | 计划中 | kid 登录后总览只读+自己打卡；papers 页对 kid 隐藏操作按钮（代码已预留 role 判断） |
| P3 | 错题独立页 | 备忘 | 错题副本 >300 条时拆页（现在折叠列表够用） |
| P3 | 跨学期/科目筛选 | 备忘 | 考卷跨 2 个学期以上、家长明确提出筛选时；subject/term 字段已就位，只差 UI |
| P3 | 多 kid | 备忘 | 新增学生时；students 表从建站起就是多学生结构，getStudentId 需扩展按 kid 取 |
| P3 | 周报推送 | 备忘 | 并入 P2 一并考虑 |

## 四、运维备忘

- papers 表已执行过 `data/migrate_papers_v9.sql`（ALTER ADD COLUMN），**勿重跑**；新环境直接用最新 schema.sql
- 自检：`python3 scripts/verify_live.py`（含考卷库 8 项断言：页可达/新建/字段/分页/编辑/删除）
- 录卷两种方式：网页「＋ 新增考卷」（家长自助）或让 agent 代写（走同一条 POST /api/papers）
